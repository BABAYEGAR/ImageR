@model CamerackStudio.Models.Entities.UserEmail
@{
    Layout = "Layout/Dashboard/_LayoutForm";
    ViewBag.PageTitle = "Create General Notice";
    ViewBag.PageHeader = "General Notice";
    ViewBag.FormHeader = "Schedule a General Notice (Make sure to fill all compulsory fields *)";
}
@section Breadcrumbs
    {
    <div class="pull-left">
        <h4 class="mt-0 mb-5">@ViewBag.PageHeader</h4>
        <ol class="breadcrumb mb-0">
            <li>
                <a href="@Url.Action("Dashboard", "Home")">Dashboard</a>
            </li>
            <li class="active">@ViewBag.PageHeader</li>
        </ol>
    </div>
}
@section css{

    <style>

        .page-header {
            padding: 0px;
        }

        .selectize-input {
            border: 0px solid #d0d0d0;
            font-weight: bolder;
            padding: 0px 0px;
            width: 100%;
        }
    </style>
}
@using (Html.BeginForm("GeneralNotice", "AppUser", FormMethod.Post, new {enctype = "multipart/form-data", role = "form"}))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "", new {@class = "text-danger"})
    <div class="form-group col-md-12">
        @Html.LabelFor(model => model.Body, new {@class = "control-label"})<span class="required">*</span>
        @Html.TextAreaFor(model => model.Body, new {@class = "form-control", id = "ckeditor-full", placeholder = "Please enter the body"})
        @Html.ValidationMessageFor(model => model.Body, "", new {@class = "text-danger"})
    </div>
    <div class="form-group">
        <button type="submit" class="btn btn-primary">Submit</button>
        <a href="@Url.Action("Dashboard","Home")"  class="btn btn-default">Cancel</a>
    </div>
        }

@section Scripts{
    <!-- CKEditor-->
    <script type="text/javascript" src="~/Dashboard/plugins/bootstrapck4-skin/ckeditor.js"></script>
    <!-- Custom JS-->
    <script type="text/javascript" src="~/Dashboard/build/js/page-content/editors/ckeditor.js"></script>
}
@section Functions
{
}